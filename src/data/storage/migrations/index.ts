import { schemaMigrations } from '@nozbe/watermelondb/Schema/migrations';

// Nessuna migrazione ancora: schema alla v1. Aggiungere qui le migrazioni
// quando lo schema cambierà, mantenendo ogni versione precedente.
export const migrations = schemaMigrations({ migrations: [] });
